using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
using Photon.Realtime;

public class PlayerDataScript : MonoBehaviour {

	/// <summary>
	/// This script is used for tracking local player data.
	/// Data should be syncronized and/or available through the network.
	/// </summary>

	// Check if playerdata is client's and if player is connected to the network.
	// if (photonView.IsMine == true && PhotonNetwork.IsConnected == true)

}
